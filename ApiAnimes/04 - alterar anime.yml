info:
  name: 04 - alterar anime
  type: http
  seq: 4

http:
  method: PUT
  url: "{{baseUrl}}/api/animes/3"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
