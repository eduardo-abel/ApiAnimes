info:
  name: 02 - listar animes
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/animes"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
