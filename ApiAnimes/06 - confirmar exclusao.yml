info:
  name: 06 - confirmar exclusao
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/api/animes/3"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
